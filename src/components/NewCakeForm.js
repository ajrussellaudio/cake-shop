import React from "react";
import PropTypes from "prop-types";

const NewCakeForm = props => <h3>NewCakeForm</h3>;

NewCakeForm.propTypes = {
  onSubmit: PropTypes.func.isRequired
};

export default NewCakeForm;
